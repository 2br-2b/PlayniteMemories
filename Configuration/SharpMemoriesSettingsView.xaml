<UserControl x:Class="SharpMemories.SharpMemoriesSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:my="clr-namespace:SharpMemories"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="ScreenshotFormatValues" 
                        MethodName="GetValues" 
                        ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="my:ScreenshotFormat"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <StackPanel>
        <CheckBox Content="Enable automatic screenshots when a game starts" IsChecked="{Binding Settings.Enabled}"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Interval (minutes):" VerticalAlignment="Center" Margin="0,6,6,6"/>
            <TextBox Width="60" Text="{Binding Settings.IntervalMinutes}" PreviewTextInput="TextBox_PreviewTextInput" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Screenshot format:" VerticalAlignment="Center" Margin="0,6,6,6"/>
            <ComboBox SelectedItem="{Binding Settings.ScreenshotFormat}" ItemsSource="{Binding Source={StaticResource ScreenshotFormatValues}}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" IsEnabled="{Binding Settings.IsJpegFormat}">
            <TextBlock Text="Jpeg quality (0-100):" VerticalAlignment="Center" Margin="0,6,6,6"/>
            <TextBox x:Name="JpegQualityText" Width="50" Text="{Binding Settings.JpegQuality, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="TextBox_PreviewTextInput" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Output folder:" VerticalAlignment="Center" Margin="0,6,6,6"/>
            <TextBox x:Name="OutputFolderText" Width="300" Text="{Binding Settings.OutputFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Browse..." Margin="6,0,0,0" Click="BrowseOutputFolder_Click" />
        </StackPanel>
        <CheckBox Content="Monitor external screenshot folder (to copy Steam screenshots to the Playnite screenshots folder)" IsChecked="{Binding Settings.EnableMonitoring}" Margin="0,6,0,0"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" IsEnabled="{Binding Settings.EnableMonitoring}">
            <TextBlock Text="Monitor folder:" VerticalAlignment="Center" Margin="0,6,6,6"/>
            <TextBox x:Name="MonitorFolderText" Width="300" Text="{Binding Settings.MonitorFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Browse..." Margin="6,0,0,0" Click="BrowseMonitorFolder_Click" />
        </StackPanel>

        <Separator Margin="0,12,0,12"/>

        <!-- Hotkey Settings -->
        <TextBlock Text="Screenshot Hotkey" FontWeight="Bold" Margin="0,0,0,6"/>
        <CheckBox Content="Enable screenshot hotkey" IsChecked="{Binding Settings.EnableHotkey}" Margin="0,0,0,6"/>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,6,0,6">
            <TextBlock Text="Current Hotkey:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBlock Text="{Binding HotkeyDisplayString}" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,12,0" MinWidth="120"/>
            <Button Content="{Binding RecordButtonText}" Click="RecordHotkey_Click" Padding="10,4" MinWidth="150"/>
        </StackPanel>

        <CheckBox Content="Suppress hotkey from reaching the application (prevents F12 from opening browser dev tools, etc.)" IsChecked="{Binding Settings.HotkeySuppressKey}" Margin="0,0,0,6"/>

        <TextBlock Text="Enable hotkey for these libraries:" Margin="0,12,0,6"/>
        <ItemsControl ItemsSource="{Binding LibraryPlugins}" Margin="20,0,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsHotkeyEnabled}" Margin="0,2"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>